import React from "react";
import BasicBadge from "../../../React-UI-Collection/src/components/ui/badges/BasicBadge.tsx";

export default function BasicBadgeTemplate() {
  return <BasicBadge variant={"primary"} size={"md"} className={""}>BasicBadge</BasicBadge>;
}
