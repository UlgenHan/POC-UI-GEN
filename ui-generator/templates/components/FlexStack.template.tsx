import React from "react";
import FlexStack from "../../../React-UI-Collection/src/components/layout/flex/FlexStack.tsx";

export default function FlexStackTemplate() {
  return <FlexStack gap={"md"} align={"stretch"} className={""}>FlexStack</FlexStack>;
}
