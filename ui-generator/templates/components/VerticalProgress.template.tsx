import React from "react";
import VerticalProgress from "../../../React-UI-Collection/src/components/ui/progress/VerticalProgress.tsx";

export default function VerticalProgressTemplate() {
  return <VerticalProgress max={"100"} height={"200"} className={""} size={"md"} variant={"default"} showPercentage={"false"}>VerticalProgress</VerticalProgress>;
}
