import React from "react";
import LabeledProgress from "../../../React-UI-Collection/src/components/ui/progress/LabeledProgress.tsx";

export default function LabeledProgressTemplate() {
  return <LabeledProgress max={"100"} showPercentage={"true"} className={""} size={"md"} variant={"default"}>LabeledProgress</LabeledProgress>;
}
