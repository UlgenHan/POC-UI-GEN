import React from "react";
import MultilineTooltip from "../../../React-UI-Collection/src/components/ui/tooltips/MultilineTooltip.tsx";

export default function MultilineTooltipTemplate() {
  return <MultilineTooltip position={"top"} maxWidth={"max-w-xs"} delay={"300"} className={""} tooltipClassName={""}>MultilineTooltip</MultilineTooltip>;
}
