import React from "react";
import ResponsiveGrid from "../../../React-UI-Collection/src/components/layout/grid/ResponsiveGrid.tsx";

export default function ResponsiveGridTemplate() {
  return <ResponsiveGrid columns={"{ base: 1"}>ResponsiveGrid</ResponsiveGrid>;
}
