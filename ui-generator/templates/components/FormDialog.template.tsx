import React from "react";
import FormDialog from "../../../React-UI-Collection/src/components/ui/dialogs/FormDialog.tsx";

export default function FormDialogTemplate() {
  return <FormDialog submitText={"Submit"} cancelText={"Cancel"} isSubmitting={"false"} className={""}>FormDialog</FormDialog>;
}
