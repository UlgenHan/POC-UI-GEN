import React from "react";
import StripedProgress from "../../../React-UI-Collection/src/components/ui/progress/StripedProgress.tsx";

export default function StripedProgressTemplate() {
  return <StripedProgress max={"100"} animated={"true"} className={""} size={"md"} variant={"default"}>StripedProgress</StripedProgress>;
}
