import React from "react";
import BasicProgress from "../../../React-UI-Collection/src/components/ui/progress/BasicProgress.tsx";

export default function BasicProgressTemplate() {
  return <BasicProgress max={"100"} className={""} size={"md"} variant={"default"}>BasicProgress</BasicProgress>;
}
