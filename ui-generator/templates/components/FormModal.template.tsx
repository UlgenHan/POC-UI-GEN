import React from "react";
import FormModal from "../../../React-UI-Collection/src/components/ui/modals/FormModal.tsx";

export default function FormModalTemplate() {
  return <FormModal submitButtonText={"Submit"} cancelButtonText={"Cancel"} loading={"false"} size={"md"} className={""}>FormModal</FormModal>;
}
