import React from "react";
import HeroSection from "../../../React-UI-Collection/src/components/layout/sections/HeroSection.tsx";

export default function HeroSectionTemplate() {
  return <HeroSection overlay={"true"} alignment={"center"} minHeight={"screen"} className={""}>HeroSection</HeroSection>;
}
