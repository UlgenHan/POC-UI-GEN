import React from "react";
import AlertDialog from "../../../React-UI-Collection/src/components/ui/dialogs/AlertDialog.tsx";

export default function AlertDialogTemplate() {
  return <AlertDialog variant={"info"} acknowledgeText={"OK"} className={""}>AlertDialog</AlertDialog>;
}
