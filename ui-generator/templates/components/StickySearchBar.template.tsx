import React from "react";
import StickySearchBar from "../../../React-UI-Collection/src/components/ui/inputs/search/StickySearchBar.tsx";

export default function StickySearchBarTemplate() {
  return <StickySearchBar placeholder={"Search..."} showOnScroll={"true"} scrollThreshold={"100"} className={""}>StickySearchBar</StickySearchBar>;
}
