import React from "react";
import TopTooltip from "../../../React-UI-Collection/src/components/ui/tooltips/TopTooltip.tsx";

export default function TopTooltipTemplate() {
  return <TopTooltip delay={"300"} className={""} tooltipClassName={""}>TopTooltip</TopTooltip>;
}
