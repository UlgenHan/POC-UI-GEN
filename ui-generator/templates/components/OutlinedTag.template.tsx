import React from "react";
import OutlinedTag from "../../../React-UI-Collection/src/components/ui/tags/OutlinedTag.tsx";

export default function OutlinedTagTemplate() {
  return <OutlinedTag className={""} size={"md"} variant={"default"}>OutlinedTag</OutlinedTag>;
}
