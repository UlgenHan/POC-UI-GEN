import React from "react";
import IconBadge from "../../../React-UI-Collection/src/components/ui/badges/IconBadge.tsx";

export default function IconBadgeTemplate() {
  return <IconBadge variant={"primary"} size={"md"} iconPosition={"left"} className={""}>IconBadge</IconBadge>;
}
