import React from "react";
import MultiStepModal from "../../../React-UI-Collection/src/components/ui/modals/MultiStepModal.tsx";

export default function MultiStepModalTemplate() {
  return <MultiStepModal initialStep={"0"} showProgress={"true"} allowSkipSteps={"false"} size={"lg"} className={""}>MultiStepModal</MultiStepModal>;
}
