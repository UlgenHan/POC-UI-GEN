import React from "react";
import BasicSpinner from "../../../React-UI-Collection/src/components/ui/loaders/BasicSpinner.tsx";

export default function BasicSpinnerTemplate() {
  return <BasicSpinner size={"md"} color={"text-blue-600"} className={""}>BasicSpinner</BasicSpinner>;
}
