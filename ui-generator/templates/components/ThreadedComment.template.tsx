import React from "react";
import ThreadedComment from "../../../React-UI-Collection/src/components/ui/comments/ThreadedComment.tsx";

export default function ThreadedCommentTemplate() {
  return <ThreadedComment replies={"[]"} level={"0"} className={""}>ThreadedComment</ThreadedComment>;
}
