import React from "react";
import StickyHeader from "../../../React-UI-Collection/src/components/layout/headers/StickyHeader.tsx";

export default function StickyHeaderTemplate() {
  return <StickyHeader logo={"{ text: Sticky"}>StickyHeader</StickyHeader>;
}
