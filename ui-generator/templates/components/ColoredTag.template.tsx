import React from "react";
import ColoredTag from "../../../React-UI-Collection/src/components/ui/tags/ColoredTag.tsx";

export default function ColoredTagTemplate() {
  return <ColoredTag variant={"default"} className={""} size={"md"}>ColoredTag</ColoredTag>;
}
