import React from "react";
import BasicAlert from "../../../React-UI-Collection/src/components/ui/alerts/BasicAlert.tsx";

export default function BasicAlertTemplate() {
  return <BasicAlert variant={"info"} className={""}>BasicAlert</BasicAlert>;
}
