import React from "react";
import ScrollableList from "../../../React-UI-Collection/src/components/ui/lists/ScrollableList.tsx";

export default function ScrollableListTemplate() {
  return <ScrollableList height={"300px"} className={""} itemClassName={""}>ScrollableList</ScrollableList>;
}
